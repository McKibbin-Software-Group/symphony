[pytest]
pythonpath = src tests
addopts = -p no:warnings
log_cli=true
log_level=DEBUG
log_format = %(asctime)s %(levelname)s %(filename)s line %(lineno)s: %(message)s
log_date_format = %H:%M:%S
python_files = *_tests.py
